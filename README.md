scrips
======

MIPS CPU designed from scratch in VHDL.

This is only an academical project, only a subset of the MIPS I instructions have been implemented.

However, this CPU works fine (each part contains unitary tests and the whole CPU has been successfully used on a FPGA to control a robot)

